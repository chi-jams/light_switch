# You should never upload a config file to a git repo, but I guess nothing here
# is particularly dangerous to have exposed and it works as a sample
server_ip: 'localhost'
server_port: 8080
hue_ip: 10.0.0.8
group: 'Chi'
